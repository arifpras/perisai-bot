version: '3.8'

services:
  bondbot:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
    command: uvicorn app_fastapi:app --host 0.0.0.0 --port 8000 --reload
